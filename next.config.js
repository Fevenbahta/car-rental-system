/** @type {import('next').NextConfig} */
const nextConfig = {}
module.exports = {
    experimental: {
      reactRoot: false,  // Disable React Server Components (if not needed)
    },
  };
  
module.exports = nextConfig
